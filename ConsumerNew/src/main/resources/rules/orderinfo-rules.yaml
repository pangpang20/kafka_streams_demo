# orderinfo 表验证规则
tables:
  - name: "orderinfo"
    rules:
      - field: "amount"
        type: "range"
        enabled: true
        min_value: 0.0
      - field: "status"
        type: "enum"
        enabled: true
        values: ["pending", "paid", "shipped", "completed", "cancelled"]
